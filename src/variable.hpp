#ifndef VAR_H
#define VAR_H

#include <vector>
#include <string>

class Vars{
public: 
    int inputNum = -1, outputNum = -1;
    std::vector<std::string> inputNames;
    std::vector<std::string> outputNames;
};

#endif
